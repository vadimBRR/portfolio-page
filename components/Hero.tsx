'use client'

import { motion } from 'framer-motion'
import Image from 'next/image'

const Hero = () => {
	return (
		<section className='h-full w-full flex flex-col lg:flex-row items-center justify-center px-6 gap-10 mt-10 lg:mt-0'>
			
		</section>
	)
}

export default Hero
